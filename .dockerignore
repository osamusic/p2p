# Git and development files
.git/
.gitignore
.github/

# Build artifacts (except what we need)
target/
!target/release/p2p-sync

# Development files
.env
.env.*
*.log
*.tmp
.DS_Store
Thumbs.db

# Docker files (not needed in build context since we copy from the builder)
docker-compose*.yml
.dockerignore

# Keep these for documentation in final image
!README.md
!LICENSE
!docs/